# Extra Python packages needed by the PowerGraph validation ON TOP OF the frozen
# Ascend environment.
#
#   pip install -r requirements-validation.txt
#
# These are only needed to read PowerGraph's MATLAB v7.3 (.mat) raw files.
# Nothing here touches torch / torch_npu / torch_geometric / numpy.
#
# ---------------------------------------------------------------------------
# DO NOT install PowerGraph's upstream requirements.txt.
#
# The upstream file pins an old CUDA stack (torch==1.9.1+cu111,
# torch_geometric==2.2.0, torch_scatter/torch_sparse CUDA builds, numpy 1.19/1.22)
# and would destroy the frozen Ascend environment.
# ---------------------------------------------------------------------------
#
# Expected base environment (NOT installed by this file, do not re-pin):
#
#   PyTorch   2.9.0        (2.9.0+cpu in the validation image)
#   torch_npu 2.9.0
#   PyG       2.8.0.post1
#   CANN      8.5.1
#   Python    3.11.14
#   numpy     2.4.6        (already satisfied; not re-pinned here)
#
h5py==3.16.0
mat73==0.65

# Not installed on purpose (see POWERGRAPH_GLOBAL_MAX_POOL_VALIDATION.md §3.1):
#   pandas / scipy / scikit-learn
# The unmodified PowerGrid loader imports
#   sklearn.model_selection.train_test_split  and  utils.gen_utils
# at module scope, but the PowerGrid code path never calls either of them.
# The validation scripts therefore register in-process stubs (scripts/pg_env.py)
# instead of pulling in those heavier dependency trees. Both stubs raise
# immediately if they are ever actually invoked.
